image_xscale = lerp(image_xscale, buttonXScale, .1)
image_yscale = lerp(image_yscale, buttonYScale, .1)

scaleTextX = lerp(scaleTextX, 1, 0.1)

scaleTextY = lerp(scaleTextY, 1, 0.1)

if(global.effectsOn)
{
	btnColor = 1
}
else
{
	btnColor = 0
}
